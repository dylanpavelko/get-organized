<h1>Listing activity_aspect_has_event_details</h1>

<table>
  <thead>
    <tr>
      <th>Activity aspect</th>
      <th>Event detail</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @activity_aspect_has_event_details.each do |activity_aspect_has_event_detail| %>
      <tr>
        <td><%= activity_aspect_has_event_detail.activity_aspect %></td>
        <td><%= activity_aspect_has_event_detail.event_detail %></td>
        <td><%= link_to 'Show', activity_aspect_has_event_detail %></td>
        <td><%= link_to 'Edit', edit_activity_aspect_has_event_detail_path(activity_aspect_has_event_detail) %></td>
        <td><%= link_to 'Destroy', activity_aspect_has_event_detail, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Activity aspect has event detail', new_activity_aspect_has_event_detail_path %>
