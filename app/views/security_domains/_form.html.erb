<%= form_for(@security_domain) do |f| %>
  <% if @security_domain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@security_domain.errors.count, "error") %> prohibited this security_domain from being saved:</h2>

      <ul>
      <% @security_domain.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
