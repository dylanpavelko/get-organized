<h1>Listing event_template_has_event_activities</h1>

<table>
  <thead>
    <tr>
      <th>Event</th>
      <th>Activities</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @event_template_has_event_activities.each do |event_template_has_event_activity| %>
      <tr>
        <td><%= event_template_has_event_activity.event %></td>
        <td><%= event_template_has_event_activity.activities %></td>
        <td><%= link_to 'Show', event_template_has_event_activity %></td>
        <td><%= link_to 'Edit', edit_event_template_has_event_activity_path(event_template_has_event_activity) %></td>
        <td><%= link_to 'Destroy', event_template_has_event_activity, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event template has event activity', new_event_template_has_event_activity_path %>
