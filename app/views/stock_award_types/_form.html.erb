<%= form_for(@stock_award_type) do |f| %>
  <% if @stock_award_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stock_award_type.errors.count, "error") %> prohibited this stock_award_type from being saved:</h2>

      <ul>
      <% @stock_award_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
