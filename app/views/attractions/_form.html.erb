<%= form_for(@attraction) do |f| %>
  <% if @attraction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attraction.errors.count, "error") %> prohibited this attraction from being saved:</h2>

      <ul>
      <% @attraction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :attraction_category_id %><br>
    <% if @attraction != nil %>
      <%= f.select :attraction_category_id, options_from_collection_for_select(AttractionCategory.all, :id, :name, @attraction.attraction_category_id), :include_blank => true %>
    <% else %>
      <%= f.select :attraction_category_id, options_from_collection_for_select(AttractionCategory.all, :id, :name), :include_blank => true %>
    <% end %>
  </div>  
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :trip_advisor_link %><br>
    <%= f.text_field :trip_advisor_link %>
  </div>
  <div class="field">
    Hotel Chain<br>
    <% if @attraction != nil %>
      <%= f.select :hotel_id, options_from_collection_for_select(HotelChain.all, :id, :name, @attraction.hotel_id), :include_blank => true %>
    <% else %>
      <%= f.select :hotel_id, options_from_collection_for_select(HotelChain.all, :id, :name), :include_blank => true %>
    <% end %>
  </div>  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
