<%= form_for(@event_agenda_item) do |f| %>
  <% if @event_agenda_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_agenda_item.errors.count, "error") %> prohibited this event_agenda_item from being saved:</h2>

      <ul>
      <% @event_agenda_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
