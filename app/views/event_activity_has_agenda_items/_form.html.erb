<%= form_for(@event_activity_has_agenda_item) do |f| %>
  <% if @event_activity_has_agenda_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_activity_has_agenda_item.errors.count, "error") %> prohibited this event_activity_has_agenda_item from being saved:</h2>

      <ul>
      <% @event_activity_has_agenda_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_activity_id %><br>
    <%= f.text_field :event_activity_id %>
  </div>
  <div class="field">
    <%= f.label :event_agenda_item_id %><br>
    <%= f.text_field :event_agenda_item_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
