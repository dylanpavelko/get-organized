<h1>Listing event_activity_has_agenda_items</h1>

<table>
  <thead>
    <tr>
      <th>Event activity</th>
      <th>Event agenda item</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @event_activity_has_agenda_items.each do |event_activity_has_agenda_item| %>
      <tr>
        <td><%= event_activity_has_agenda_item.event_activity %></td>
        <td><%= event_activity_has_agenda_item.event_agenda_item %></td>
        <td><%= link_to 'Show', event_activity_has_agenda_item %></td>
        <td><%= link_to 'Edit', edit_event_activity_has_agenda_item_path(event_activity_has_agenda_item) %></td>
        <td><%= link_to 'Destroy', event_activity_has_agenda_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event activity has agenda item', new_event_activity_has_agenda_item_path %>
