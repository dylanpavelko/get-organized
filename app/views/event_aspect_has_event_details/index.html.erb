<h1>Listing event_aspect_has_event_details</h1>

<table>
  <thead>
    <tr>
      <th>Activity aspect</th>
      <th>Event detail</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @event_aspect_has_event_details.each do |event_aspect_has_event_detail| %>
      <tr>
        <td><%= event_aspect_has_event_detail.activity_aspect %></td>
        <td><%= event_aspect_has_event_detail.event_detail %></td>
        <td><%= link_to 'Show', event_aspect_has_event_detail %></td>
        <td><%= link_to 'Edit', edit_event_aspect_has_event_detail_path(event_aspect_has_event_detail) %></td>
        <td><%= link_to 'Destroy', event_aspect_has_event_detail, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event aspect has event detail', new_event_aspect_has_event_detail_path %>
