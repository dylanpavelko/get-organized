<%= form_for(@subcategory) do |f| %>
  <% if @subcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subcategory.errors.count, "error") %> prohibited this subcategory from being saved:</h2>

      <ul>
      <% @subcategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :category_id %><br>
    <% if @subcategory != nil %>
      <%= f.select :category_id, options_from_collection_for_select(Category.all, :id, :category, @subcategory.category_id), :include_blank => true %>
    <% else %>
      <%= f.select :category_id, options_from_collection_for_select(Category.all, :id, :category), :include_blank => true %>
    <% end %>
  </div>
  <div class="field">
    <%= f.label :subcategory %><br>
    <%= f.text_field :subcategory %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
