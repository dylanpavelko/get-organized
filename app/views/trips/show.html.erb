<p id="notice"><%= notice %></p>

<p>
  <strong>Trip: <%= @trip.name %></strong>
  <br/>
  <%= @trip.description %>
</p>

<p>
  <strong>Blog:</strong>
  <%= @trip.blog_link %>

  <strong>Photo:</strong>
  <%= @trip.photo_link %>

  <% if @trip.tripit_link != '' %>
    <%= link_to 'Tripit Link', @trip.tripit_link, target: "_blank" %>

  <% end %>

</p>

<!-- LIST FORMAT OF ITINERARY
<p>
  <strong>Itinerary:</strong>
  <% if @days.count > 0 %>
  Days: <%= @days.count %>
  <ul>
    <% @days.each do |day| %>
      <% day.each do |activity| %>
        <% if activity == day.first %>
          <strong><%= activity.date %></strong>
        <% end %>
        <li> 
          <%= activity.datetime.strftime("%I:%M %p - ") %>
          <%= link_to activity.get_name, activity.activity %> 
          <% if activity.airline != nil %> 
            <%= link_to image_tag(activity.airline.image, height: '12'), activity.airline %>
          <% elsif activity.attraction != nil and activity.attraction.hotel != nil%>
            <%= link_to image_tag(activity.attraction.hotel.image, height: '12'), activity.attraction.hotel %>
          <% end %>
          <%= link_to 'edit', edit_itinerary_activity_path(activity) %>
        </li>
      <% end %>
    <% end %>
  </ul>
  <a href='#'>Add Itinerary Activity</a>  <%= link_to 'Temporary Add Activity' , new_itinerary_activity_path %>
<% end %>
</p> -->



<strong>Itinerary:</strong>
<% @no_date_activities.each do |activity| %>
-No Date: <%= activity.get_name %><br/>
<% end %>
<% if @days.count > 0 %>
  Days(<%= @days.count %>)
<div class='container'>
<div class='schedule'>
  <span class='travel_header'><strong>Travel</strong></span>
  <span class='lodging_header'><strong>Lodging</strong></span>
  <span class='activity_header'><strong>Activities</strong></span>
  <span class='activity_header'><strong>Dining</strong></span>
  <span class='activity_header'><strong>Pack</strong></span>
  <% activity_count = 0 %>
  <% @days.each do |day| %>
      <% day.each do |activity| %>
        <% if activity == day.first %>
        <div class='day' id=<%= 'day' + (@days.index(day)+1).to_s %>>
          <span class='datename'><strong><%= activity.date %></strong></span>

        <% end %>
        <div class=
        <% if activity.attraction != nil and activity.attraction.attraction_category != nil and activity.attraction.attraction_category.name == "Hotel" %>
          "item lodging"
        <% elsif activity.airline != nil %>
          "item travel"
        <% else %>
          "item activity"
        <% end %>
        id=<%= activity_count = activity_count + 1 %>>
          <%= activity.datetime.try(:strftime, "%I:%M %p - ") %>
                <% if activity.airline != nil %> 
                   <%= link_to image_tag(activity.airline.image, height: '12'), activity.airline %>
                <% elsif activity.attraction != nil and activity.attraction.hotel != nil%>
                  <%= link_to image_tag(activity.attraction.hotel.image, height: '12'), activity.attraction.hotel %>
                <% end %>
                <br/>
                <%= link_to activity.get_name, activity.activity %> 
                <%= link_to 'edit', edit_itinerary_activity_path(activity.activity) %>
        </div>
      <% end %>
              </div>
    <% end %>
</div>
</div>
    <a href='#'>Add Itinerary Activity</a>  <%= link_to 'Temporary Add Activity' , new_itinerary_activity_path %>
<% end %>
<br/><br/><br/>
<div>


<div id='bottom'>
<p>
  <strong>Pack List:</strong>
  <% if @trip_items.count > 0 %>
  <ul>
    <% @trip_items.each do |item| %>
      <li><%= link_to item.inventory_item.full_name, item.inventory_item %> <a href='#'>remove</a></li>
    <% end %>
  </ul>
  <% end %>
  <a href='#'>Add Item</a>  <%= link_to 'Temporary Add Item' , new_trip_has_inventory_item_path %>

</p>
</div>
<div>



</script>

<%= link_to 'Edit', edit_trip_path(@trip) %> |
<%= link_to 'All Trips', trips_path %>
