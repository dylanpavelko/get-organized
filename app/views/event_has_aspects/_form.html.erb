<%= form_for(@event_has_aspect) do |f| %>
  <% if @event_has_aspect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_has_aspect.errors.count, "error") %> prohibited this event_has_aspect from being saved:</h2>

      <ul>
      <% @event_has_aspect.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_id %><br>
    <%= f.text_field :event_id %>
  </div>
  <div class="field">
    <%= f.label :planning_aspect_id %><br>
    <%= f.text_field :planning_aspect_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
