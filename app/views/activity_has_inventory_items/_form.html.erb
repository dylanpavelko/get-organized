<%= form_for(@activity_has_inventory_item) do |f| %>
  <% if @activity_has_inventory_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@activity_has_inventory_item.errors.count, "error") %> prohibited this activity_has_inventory_item from being saved:</h2>

      <ul>
      <% @activity_has_inventory_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :activity_id %><br>
    <% if @activity_has_inventory_item != nil %>
      <%= f.select :activity_id, options_from_collection_for_select(Activity.all, :id, :name, @activity_has_inventory_item.activity_id), :include_blank => true %>
    <% else %>
      <%= f.select :activity_id, options_from_collection_for_select(Activity.all, :id, :name), :include_blank => true %>
    <% end %>
  </div>
  <div class="field">
    <%= f.label :inventory_item_id %><br>
    <% if @activity_has_inventory_item != nil %>
      <%= f.select :inventory_item_id, options_from_collection_for_select(InventoryItem.all, :id, :full_name, @activity_has_inventory_item.inventory_item_id), :include_blank => true %>
    <% else %>
      <%= f.select :inventory_item_id, options_from_collection_for_select(InventoryItem.all, :id, :full_name), :include_blank => true %>
    <% end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
