<%= form_for(@planning_aspect) do |f| %>
  <% if @planning_aspect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@planning_aspect.errors.count, "error") %> prohibited this planning_aspect from being saved:</h2>

      <ul>
      <% @planning_aspect.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :event_type_id %><br>
    <% if @planning_aspect.event_type != nil %>
      <%= f.select :event_type_id, options_from_collection_for_select(EventType.all, :id, :name, @event_template.event_type_id), :include_blank => true %>
    <% else %>
      <%= f.select :event_type_id, options_from_collection_for_select(EventType.all, :id, :name), :include_blank => true %>
    <% end %>
  </div>
  <div class="field">
    <%#= f.label :event_activity_id %><br>
    <%#= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
