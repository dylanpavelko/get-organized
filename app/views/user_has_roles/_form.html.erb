<%= form_for(@user_has_role) do |f| %>
  <% if @user_has_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_has_role.errors.count, "error") %> prohibited this user_has_role from being saved:</h2>

      <ul>
      <% @user_has_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    User:<br>
    <% if @user_has_role.user_id != nil %>
      <%= f.select :user_id, options_from_collection_for_select(User.all, :id, :username, @user_has_role.user_id), :include_blank => true %>
    <% else %>
      <%= f.select :user_id, options_from_collection_for_select(User.all, :id, :username), :include_blank => true %>
    <% end %>
  </div>
  <div class="field">
    Role:<br>
    <% if @user_has_role.role_id != nil %>
      <%= f.select :role_id, options_from_collection_for_select(Role.all, :id, :name, @user_has_role.role_id), :include_blank => true %>
    <% else %>
      <%= f.select :role_id, options_from_collection_for_select(Role.all, :id, :name), :include_blank => true %>
    <% end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
