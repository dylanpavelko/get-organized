<%= form_for(@stock_purchase_type) do |f| %>
  <% if @stock_purchase_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stock_purchase_type.errors.count, "error") %> prohibited this stock_purchase_type from being saved:</h2>

      <ul>
      <% @stock_purchase_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
